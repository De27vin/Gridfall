# APK download

Place the signed Android release APK here as `gridfall.apk` before deploying the website:

```text
website/downloads/gridfall.apk
```

The landing page links to `/downloads/gridfall.apk`. The APK is intentionally not committed to this repository.